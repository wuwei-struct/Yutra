# create_return_request

Create return requests through `adapters/return-adapter.mjs`.

- Category: return operation
- Side effect: write
- Risk: medium
- Requires approval: false

This skill keeps return creation logic in adapter layer.
