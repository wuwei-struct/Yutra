# Yutra v0.4 Preview RC Smoke

## Candidate

- Tested base commit: `d434cf1`
- Tested change set: P6-12B fixed-version alignment and RC evidence working tree
- Package version: `0.4.0-vnext-preview.1`
- Candidate tag: `v0.4.0-vnext-preview.1`
- Package manifests: `27`
- Lockfile change: none
- Smoke date: `2026-08-02`

## Result

```yaml
automatedGatesReady: true
cliSmokeReady: true
browserSmokeReady: true
evidenceReplayReady: true
releaseSmokeReady: true
releaseTagReady: false
releaseTagBlocker: release_notes_and_publication_prep
runtimeExecutedDuringReplay: false
externalEffectsOccurred: false
temporaryArtifactsCleaned: true
tagCreated: false
githubReleaseCreated: false
npmPublished: false
```

This evidence records an in-memory, mock-only, single-process RC smoke against
canonical demos. It is not production readiness evidence.

## Automated Gates

The aligned candidate passed installation, lint, test, build, certification,
verification, and frozen-lockfile installation. The final documentation and
Conformance change set is rerun through the same release gates before commit.

## Creator CLI Smoke

| Product Archetype | Dry run | Force output | Canonical artifacts | Compile report | DSL inspect | Boundary |
| --- | --- | --- | --- | --- | --- | --- |
| `request-resolution` | passed | passed | 6 | passed | passed | mock; `containsRealEndpoint=false`; `containsSecret=false` |
| `approval-decision` | passed | passed | 6 | passed | passed | mock; `containsRealEndpoint=false`; `containsSecret=false` |
| `knowledge-answering` | passed | passed | 6 | passed | passed | mock; `containsRealEndpoint=false`; `containsSecret=false` |

## Composition And Orchestrator CLI Smoke

| Scenario | Composition | Orchestrator | Result |
| --- | --- | --- | --- |
| customer complaint | 3 Slots, 7 composition artifacts, 6 artifacts per Slot | 6 Orchestrator artifacts | `previewOnly=true`; `runtimeExecutable=false`; `currentRuntimeSupported=false` |
| ecommerce refund | 2 Slots, 7 composition artifacts, 6 artifacts per Slot | 6 Orchestrator artifacts | `previewOnly=true`; `runtimeExecutable=false`; `currentRuntimeSupported=false` |
| renewal churn | `COMPOSITION_NOT_COMPILE_READY` | `ORCHESTRATOR_COMPOSITION_NOT_READY` | fail-closed; no partial artifacts |

Both successful Orchestrator bundles contained `scenario.orchestrator.yaml`,
contained no top-level `agent.yutra.yaml`, and kept Slot artifacts namespaced.
No Pack Config deep merge occurred.

## Creator Browser Smoke

`request-resolution`, `approval-decision`, and `knowledge-answering` each
passed field editing, `confirmedByUser` provenance, Rule Impact, Compile
Preview, six canonical artifacts, Certification Readiness, and Send to DSL
Editor. Sending a Slot DSL did not automatically Inspect, Apply, or Run.

## Composition And Orchestrator Browser Smoke

- customer complaint: Composition and Orchestrator previews passed; 3 Slot
  namespaces, 7 composition artifacts, and 6 Orchestrator artifacts visible;
- ecommerce refund: Composition and Orchestrator previews passed; 2 Slot
  namespaces, 7 composition artifacts, and 6 Orchestrator artifacts visible;
- renewal churn: contract-only blockers remained visible and Composition,
  Orchestrator, and Run controls remained disabled;
- `scenario.orchestrator.yaml` was explicitly identified as not Agent DSL and
  had no Apply, Run, or Agent DSL Editor bridge;
- no deep merge, implicit adapter inheritance, or secret merge was exposed.

## Manual Scenario Runs

| Case | Status | Terminal | Slot invocations | Evidence |
| --- | --- | --- | --- | --- |
| `complaint_policy` | completed | `$scenario_done` | 3 | Projection, routes, binding, overlays, budget, redacted audit |
| `complaint_compensation` | completed | `$scenario_done` | 3 | Supporting authorization, binding, terminal evidence |
| `complaint_handoff` | handoff_required | `$human_handoff` | 1 | Handoff projection and redacted audit |
| `refund_authorization` | completed | `$scenario_done` | 3 | Authorization projection, binding, terminal evidence |
| `overlay_deny` | failed | `$fail_closed` | 0 | Overlay deny and fail-closed evidence |

All five runs reported `externalEffectsOccurred=false`. Runs were initiated
only by the manual **Run Scenario Preview** action; Composition and
Orchestrator compilation did not trigger a run.

## Evidence Replay

All five Evidence Bundles passed integrity inspection and offline replay:

- `complaint_policy`
- `complaint_compensation`
- `complaint_handoff`
- `refund_authorization`
- `overlay_deny`

Each replay reported `replayMode=offline_evidence` and
`runtimeExecuted=false`. Replay added zero Scenario Run API requests: server
run IDs remained sequential from `studio-scenario-preview-1` through
`studio-scenario-preview-5`, with no replay-created run or ID gap. Slot tree,
decision summaries, terminal, budget, provenance, redaction, and audit summary
were visible.

## Public Boundary And Cleanup

- no real side effects;
- no persistence;
- no real customer adapter;
- no real LLM, RAG, or knowledge base;
- no production claim;
- no Tag, GitHub Release, or npm publication;
- `.tmp/v0.4-rc-smoke`, logs, service processes, and browser state were cleaned;
- Studio and Runner smoke ports were closed after validation.

The existing `v0.3.0-vnext-preview.1` tag was not changed. The v0.4 candidate
tag remains absent and `releaseTagReady=false` until release notes and
publication preparation are completed.
