# Yutra v0.4 Preview Release Readiness

## Candidate

- Candidate version: `0.4.0-vnext-preview.1`
- Candidate tag: `v0.4.0-vnext-preview.1`
- Audited branch: `main`
- Tested base commit: `d434cf1`
- Current package version: `0.4.0-vnext-preview.1`
- Package manifests: `27` total (`1` root and `26` workspace packages)
- Existing published tag: `v0.3.0-vnext-preview.1`
- Existing published tag commit: `90c006e3caddeb2395c0badb2d2dfb9c18b91451`
- RC smoke evidence: [v0.4 Preview RC Smoke](v0.4-preview-rc-smoke.md)
- Release type: GitHub prerelease

## Readiness State

```yaml
releaseScopeFrozen: true
automatedGatesReady: true
browserSmokeReady: true
cliSmokeReady: true
evidenceReplayReady: true
versionAligned: true
releaseSmokeReady: true
releaseTagReady: false
releaseTagBlocker: release_notes_and_publication_prep
tagCreated: false
githubReleaseCreated: false
npmPublished: false
```

The v0.4 Preview scope, fixed package version, automated gates, CLI smoke, and
browser RC smoke are aligned. Release tagging remains fail-closed until exact
release notes and publication preparation are completed in a separate task.

## Release Scope

The v0.4 Preview candidate includes:

1. Scenario Pattern Core.
2. Scenario Composition Contract, Compiler, and Studio Compile Preview.
3. Scenario Orchestrator Contract, Compiler, and Studio Preview.
4. Scenario Orchestrator Runtime Adapter Contract.
5. In-memory Demo Runtime Adapter for isolated Slot execution.
6. Explicit Slot Outcome Projection contracts and evidence.
7. Dispatch-time side-effect enforcement with fail-closed classification.
8. In-memory Scenario Orchestrator Engine for canonical demos.
9. Studio Manual Scenario Run Preview.
10. Redacted Scenario Run Evidence Bundles.
11. Offline Evidence Replay and integrity inspection.

Customer complaint and ecommerce refund are the canonical executable demos.
Renewal churn remains contract-only and is not compile- or run-enabled.

## Explicit Exclusions

- production Runtime Adapter;
- distributed Orchestrator, multi-process coordination, or durable queues;
- Snapshot / Resume or cancellation;
- persistent Audit, Evidence storage, run history, or remote synchronization;
- arbitrary Scenario Plan Authoring;
- real customer adapters, data, SOP, or business rules;
- real LLM, RAG, or knowledge base integration;
- real write, external, financial, or approval side effects;
- multi-tenant identity, authorization, or administration systems;
- production certification or production readiness;
- npm publication.

## Public Boundary

v0.4 Preview remains:

- prerelease;
- mock-only;
- in-memory;
- single-process;
- canonical demo only;
- no real side effects;
- no persistence;
- no real customer adapters;
- no real LLM / RAG / knowledge base;
- not production ready;
- npm not published.

Evidence is a redacted integrity-checked summary, not a production Audit
platform. Offline replay reads existing evidence and never re-executes Runtime
or the Scenario Engine.

## Repository And Version Audit

- The root package and all 26 workspace packages are fixed at
  `0.4.0-vnext-preview.1` (`versionAligned=true`).
- Workspace dependencies remain on their existing `workspace:*` protocols.
- The lockfile required no metadata change and no dependency was upgraded.
- The candidate tag does not exist locally or on `origin`.
- The published `v0.3.0-vnext-preview.1` tag remains fixed to its release
  commit.

## Automated Gates

The aligned candidate passed:

```text
pnpm install --config.confirmModulesPurge=false
pnpm lint
pnpm test
pnpm build
pnpm certify
pnpm verify
pnpm install --frozen-lockfile
```

The final documentation and Conformance change set is gated again before
commit. Certification Summary remains generated evidence and does not imply
production readiness.

## CLI Smoke Matrix

| Area | Result |
| --- | --- |
| Creator compiler | request-resolution, approval-decision, and knowledge-answering passed dry-run, force output, six canonical artifacts, compile report, DSL inspect, and mock boundary checks |
| Composition compiler | customer complaint (3 Slots) and ecommerce refund (2 Slots) passed with seven top-level artifacts and six artifacts per Slot |
| Composition rejection | renewal churn returned `COMPOSITION_NOT_COMPILE_READY` and wrote no partial artifacts |
| Orchestrator compiler | customer complaint and ecommerce refund produced six Orchestrator artifacts and `scenario.orchestrator.yaml` without a top-level Agent DSL |
| Orchestrator rejection | renewal churn returned `ORCHESTRATOR_COMPOSITION_NOT_READY` and wrote no partial artifacts |
| Cleanup | `.tmp/v0.4-rc-smoke` and service logs were removed |

## Browser Smoke Matrix

| Area | Result |
| --- | --- |
| Single Creator | all three enabled Product Archetypes passed field editing, confirmed user provenance, Rule Impact, Compile Preview, six artifacts, Certification Readiness, and manual Slot DSL bridge checks |
| Scenario Composition | customer complaint and ecommerce refund passed; renewal churn remained contract-only; seven top-level and six per-Slot artifacts were visible; no deep merge |
| Orchestrator Preview | customer complaint and ecommerce refund passed with six artifacts; renewal churn remained blocked; Orchestrator YAML was explicitly not Agent DSL and had no Apply, Run, or editor bridge |
| Manual Scenario Run | complaint policy and compensation completed; complaint handoff reached `$human_handoff`; refund authorization completed; overlay deny reached `$fail_closed` with zero Slot invocations |
| Evidence Replay | all five cases replayed with valid integrity, `replayMode=offline_evidence`, and `runtimeExecuted=false` |
| Replay network boundary | Replay added zero Scenario Run API requests; sequential server run IDs remained `1` through `5` with no replay-created gaps |
| UI and safety | English and Chinese UI passed; browser error/warn log was empty; no automatic Run or real external request occurred |

## Known Limitations

- The demo Engine and Adapter are in-memory and single-process.
- Only canonical demo plans, profiles, cases, handlers, and markers are allowed.
- Renewal churn is contract-only.
- Evidence integrity is deterministic hashing, not a trusted digital signature
  or durable Audit store.
- Runtime execution is not production-hardened, distributed, resumable, or
  persistent.

## Release Blocker

`releaseTagReady=false` remains mandatory. The sole next-stage blocker is
`release_notes_and_publication_prep`: exact-version release notes and public
prerelease publication materials must be reviewed before an annotated tag is
created.

## Recommended Next Task

Proceed to **P6-12C Release Notes, Annotated Tag, and GitHub Prerelease** only
after reviewing the RC smoke evidence. Tag creation, GitHub prerelease
creation, and npm publication were not performed by P6-12B.
