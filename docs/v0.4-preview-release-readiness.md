# Yutra v0.4 Preview Release Readiness

## Candidate

- Candidate version: `0.4.0-vnext-preview.1`
- Candidate tag: `v0.4.0-vnext-preview.1`
- Audited branch: `main`
- Audited HEAD: `1efacbd`
- Current package version: `0.3.0-vnext-preview.1`
- Existing published tag: `v0.3.0-vnext-preview.1`
- Existing published tag commit: `90c006e3caddeb2395c0badb2d2dfb9c18b91451`
- Release type: GitHub prerelease

## Readiness State

```yaml
releaseScopeFrozen: true
automatedGatesReady: true
browserSmokeReady: false
versionAligned: false
releaseTagReady: false
tagCreated: false
githubReleaseCreated: false
npmPublished: false
```

This audit freezes the intended v0.4 Preview scope. It does not align package
versions, run the full browser release smoke, create a tag, create a GitHub
Release, or publish npm packages.

## Release Scope

The v0.4 Preview candidate includes the following public, governed demo
capabilities developed after `v0.3.0-vnext-preview.1`:

1. Scenario Pattern Core.
2. Scenario Composition Contract, Compiler, and Studio Compile Preview.
3. Scenario Orchestrator Contract, Compiler, and Studio Preview.
4. Scenario Orchestrator Runtime Adapter Contract.
5. In-memory Demo Runtime Adapter for isolated Slot execution.
6. Explicit Slot Outcome Projection contracts and evidence.
7. Dispatch-time side-effect enforcement with fail-closed classification.
8. In-memory Scenario Orchestrator Engine for canonical demos.
9. Studio Manual Scenario Run Preview.
10. Redacted Scenario Run Evidence Bundles.
11. Offline Evidence Replay and integrity inspection.

The current candidate supports canonical customer complaint and ecommerce
refund demo paths. Renewal churn remains contract-only and is not compile- or
run-enabled.

## Explicit Exclusions

The following capabilities are not part of this candidate:

- production Runtime Adapter;
- distributed Orchestrator, multi-process coordination, or durable queues;
- Snapshot / Resume or cancellation;
- persistent Audit, Evidence storage, run history, or remote synchronization;
- arbitrary Scenario Plan Authoring;
- real customer adapters, endpoints, data, SOP, or business rules;
- real LLM, RAG, or knowledge base integration;
- real write, external, financial, or approval side effects;
- multi-tenant identity, authorization, or administration systems;
- production certification or production readiness;
- npm publication.

## Public Boundary

v0.4 Preview remains:

- prerelease;
- mock-only;
- in-memory;
- single-process;
- canonical demo only;
- no real side effects;
- no persistence;
- no real customer adapters;
- no real LLM / RAG / knowledge base;
- not production ready;
- npm not published.

Evidence is a redacted integrity-checked summary, not a production Audit
platform. Offline replay reconstructs existing evidence and never re-executes
Runtime or the Scenario Engine.

## Repository And Version Audit

- Root and all 26 workspace packages are currently fixed at
  `0.3.0-vnext-preview.1`.
- The candidate version is `0.4.0-vnext-preview.1`, so `versionAligned=false`.
- The candidate tag does not exist locally or on `origin` at the audited HEAD.
- The published `v0.3.0-vnext-preview.1` tag remains fixed to its original
  release commit.
- No package version or dependency version is changed by this audit.

## Automated Gate Status

The release audit requires all of the following commands to pass from a clean
workspace:

```text
pnpm lint
pnpm test
pnpm build
pnpm certify
pnpm verify
pnpm install --frozen-lockfile
```

This audit run recorded all six commands as passing, so
`automatedGatesReady=true`. Certification Summary is generated evidence only
and does not make the candidate production ready. Browser and CLI RC smoke are
still separate blockers and were not rerun by this audit.

## Required Browser Smoke Matrix

The next RC task must run the complete browser matrix. Prior iteration smoke
evidence is useful regression context but does not set `browserSmokeReady=true`
for this candidate.

| Area | Required checks |
| --- | --- |
| Single Creator | `request-resolution`, `approval-decision`, and `knowledge-answering` compile and manual preview paths |
| Scenario Composition | customer complaint and ecommerce refund preview; renewal churn remains contract-only |
| Orchestrator Preview | customer complaint and ecommerce refund preview; renewal churn is blocked |
| Manual Scenario Run | complaint policy completed; complaint compensation completed; complaint handoff; refund authorization completed; overlay deny fail-closed |
| Evidence Replay | all five cases replay offline; Replay adds zero Scenario Run API requests; `runtimeExecuted=false` |
| Boundaries | no real external request or side effect; no automatic run; no persistence; English and Chinese UI; no console or page errors |

## Required CLI Smoke Matrix

| Area | Required checks |
| --- | --- |
| Creator compiler | compile the canonical request-resolution, approval-decision, and knowledge-answering Pack Configs |
| Composition compiler | compile customer complaint and ecommerce refund preview bundles; confirm renewal churn rejection |
| Orchestrator compiler | compile customer complaint and ecommerce refund preview bundles; confirm renewal churn rejection |
| Output boundaries | deterministic hashes, namespaced Slot artifacts, no deep merge, no executable top-level Agent DSL, and no generated artifacts left in Git |

All CLI smoke uses demo/mock inputs. It must not publish npm packages, upload
artifacts, connect external systems, or modify a release tag.

## Documentation Consistency Audit

The audit checked README navigation, CHANGELOG `Unreleased`, the roadmap, the
published v0.3 candidate record, Scenario Engine, Studio Manual Run, Evidence
Replay, and public/open-source boundary documents.

No conflicting blanket claim was found that the Scenario Engine or Evidence
Replay is absent from current `main`. Statements that the current production
Runtime cannot execute the Orchestrator, or that the one-Slot demo Adapter is
not itself the Scenario Engine, remain correctly scoped and were preserved.

## Known Limitations

- The demo Engine and Adapter are in-memory and single-process.
- Only canonical demo plans, profiles, cases, handlers, and markers are allowed.
- Renewal churn is contract-only.
- Evidence integrity uses deterministic hashing, not a trusted digital
  signature or durable Audit store.
- Runtime execution is not production-hardened, distributed, resumable, or
  persistent.
- The current package version still identifies the v0.3 preview baseline.

## Release Blockers

1. Align the root and every workspace package to
   `0.4.0-vnext-preview.1` without changing behavior.
2. Run and record the complete browser smoke matrix.
3. Run and record the complete CLI smoke matrix after version alignment.
4. Re-run all automated gates on the aligned candidate.
5. Prepare exact-version release notes and publication status documents.
6. Only after all blockers close, set `releaseTagReady=true` and create an
   annotated tag in a separate explicit release task.

## Recommended Next Tasks

Proceed to **P6-12B v0.4 Version Alignment & Full RC Smoke**. That task should
align versions, run the browser and CLI matrices, capture smoke evidence, and
re-evaluate `releaseTagReady`. Tag creation, GitHub prerelease creation, and npm
publication remain separate explicit decisions.
